---
description: Umbra HackCMU — all agents and phase Tasks MUST use Grok 4.6 xhigh FAST
alwaysApply: true
---

# Umbra: Grok 4.6 xhigh FAST is mandatory

The human (2026-09-12 07:12 ET) switched all future work to fast mode, then (07:18 ET) switched the builder from Composer 2.5 to **Grok 4.6 xhigh FAST**. That **is** the explicit ask in `no-fast-mode-agents.mdc`. Treat **every turn in this repo** as already having “use fast mode” and “use grok 4.6 xhigh FAST” in the message. Do not wait for them to repeat it.

## Required

- Every Task / subagent / phase builder: model **`cursor-grok-4.6-xhigh-fast`** (Grok 4.6 xhigh FAST).
- Never spawn Composer 2.5 (`composer-2.5` or `composer-2.5-fast`) or Fable as the overnight builder.
- Fable 5.1 remains compile-stuck only (one Task + one retry), then next stack / `LOCAL_*`.
- Do not wait for the human to repeat this on later turns. This file is the standing order until they say otherwise.

Off only if they say: stop fast mode / use composer / normal mode / not grok.
