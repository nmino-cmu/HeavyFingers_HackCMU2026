# Umbra P8 — FHE sealed bid Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Encrypt two amounts; worker FHE-compares; client decrypts winner index (S24). Logs never contain `7391` or `12457`.

**Architecture:** Concrete/OpenFHE on `$UMBRA_FHE_VPC_IP:8085`. Single-key Concrete means the key holder can see both bids — say that in the test docstring. Server still must not see plaintext amounts.

**Tech Stack:** Same FHE stack as P2.

## Global Constraints

Obey RULES. Requires **P4-DONE**. This is sealed **amounts**, not the hop wallet (that is P9). Try Concrete **and** OpenFHE; local Mac compare only if both fail. Never plaintext prices on the worker.

**Orch:** add `POST /bid` → `$UMBRA_FHE_VPC_IP:8085`. Redeploy orch. UFW. Bind `$UMBRA_FHE_VPC_IP` only.

---

### Task 1

**Files:** `umbra/test_bid.py`, `umbra/worker/bid_server.py`

- [ ] enc(7391) vs enc(12457) → idx 1; swapped → 0; stated tie policy; two-key; int64 POST → 4xx.
- [ ] Deploy + tests + XOR. `umbra/phases/P8-DONE.md`.
