# Umbra P4 — Card, Whisper S1, S19 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Public video card, local Whisper exact-order nonce (S1), AND of FHE bits + S1, abort if decrypted bit ≠ local predicate (S19). No hop keys on a fail.

**Architecture:** Card is generated on the Mac (clear). Whisper never goes to Vultr. `decide()` is local. FHE bits still from P3 worker.

**Tech Stack:** `whisper` or `faster-whisper` **on the Mac only**. Existing FHE client.

## Global Constraints

Obey RULES. **Builder:** Grok 4.6 xhigh FAST (`cursor-grok-4.6-xhigh-fast`, empty history). Requires P3-DONE (or P2-DONE if P3 was local/partial — AND over bits that exist). **Verification phase — no Solana.** POST of the nonce string to orch/worker is 4xx. After this file is green, the next Goal is P9 if the clock is short, else P5.

---

### Task 1: Tests

**Files:**
- Create: `umbra/test_s1_s19.py`
- Create: `umbra/s1.py`, `umbra/decide.py`, `umbra/card.py`

**Interfaces:**
- `s1(transcript: str, nonce: str) -> bool` — exact words in order; capitalize+period still True; swap “fox dog” False
- After `import umbra.s1`, `requests` and `urllib.request` must not be in `sys.modules`
- `decide(fhe_bits, local_bits, s1_ok) -> Result` — pass only all-1 AND S1; any single-bit flip fails; FHE≠local either way → abort; abort ⇒ empty keydir, zero RPC

- [ ] **Step 1: Write failing tests.**
- [ ] **Step 2: cursor-grok-4.6-xhigh-fast audit.**
- [ ] **Step 3: Implement** card generator + Whisper + decide. Minimal UI later (P9). A CLI `python3 -m umbra.card` is enough.
- [ ] **Step 4: Run s1/s19 + prior tests against Vultr URL.** Codex XOR (worker still must not see the nonce). `umbra/phases/P4-DONE.md`.

This is the **minimum correct product** with P2+P3. Later phases are extra screens. Do not start P5 in this Goal.
