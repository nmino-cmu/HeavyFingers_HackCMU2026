# P6 XOR (Codex Sol)

Runtime path: `audio_server.py` rejects plaintext-shaped bodies, unpacks `evk`+ct, calls only Concrete `FHEModelServer.run`. Decrypt is Mac-side. Torch / `reference_s2` / 64×64 mel stay in compile and tests. No face / `.xyt` / `sk` on the worker.

`codex exec --ephemeral -s read-only -m gpt-5.6-sol` (2026-09-12).

VERDICT: PASS
