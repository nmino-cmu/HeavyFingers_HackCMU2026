# P5 XOR (Codex Sol)

Runtime path: Concrete `Server.run((probe, tmpl), evaluation_keys=evk)` on farm-fast. Client encrypts `.xyt` crop and decrypts the bit. Worker rejects plaintext `.xyt` / `v`. Templates at rest are ciphertext. No `sk` / `client.zip` under `/opt/umbra/print`.

```
No. The worker rejects plaintext and only performs TFHE ciphertext evaluation with the matching evaluation key; it neither decrypts nor classifies plaintext `.xyt`, `v`, or `sk`.

PASS
```
